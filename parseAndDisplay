async function displayData(dataObj) {
  // Deal with dataObj here
  console.log(displayData)
}

document.onLoad = () => {
  fetch("projectTelescope/view")
    .then(response => response.json())
    .then(displayData);
}
